第一天：字符串 + 进制
HJ5 进制转换（16→10，简单版）
HJ11 数字颠倒（字符串反转）
HJ12 字符串反转（整串反转）
HJ46 截取字符串（按长度截取）
HJ87 密码强度等级（字符串判断）
HJ106 字符逆序（输入一行，逆序输出）
1. 数字 ↔ 字符转换
- 数字转字符：`数字 + '0'`
- 字符转数字：`字符 - '0'`
2. 字符串反转
- 函数：`reverse()`
- 用法：`reverse(s.begin(), s.end());`
3. 截取字符串
- 函数：`substr()`
- 用法：`s.substr(起始下标, 长度)`
4. 整行输入（含空格）
- 函数：`getline()`
- 用法：`getline(cin, s);`
5. 进制转换（16→10）
- 字符转10进制：
  - 数字：`c - '0'`
  - 大写：`c - 'A' + 10`
6. 幂函数
- 函数：`pow()`
- 用法：`pow(底数, 指数)` → 结果是double
7. 数字判断
- 函数：`isdigit(c)`
- 作用：判断是否为数字字符9
8. 大写字母判断
- 函数：`isupper(c)`
- 作用：判断是否为大写字母
9. 小写字母判断
- 函数：`islower(c)`
- 作用：判断是否为小写字母
10. 判断是不是字母
isalpha(c);