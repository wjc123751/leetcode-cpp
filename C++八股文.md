# C++ 八股文
## 一、C++基础
### 讲一讲封装、继承、多态是什么
封装:将数据和操作数据的方法放在一起，对外留出接口
继承:子类复用父类的代码，扩展新功能
多态:一个接口，不同子类有不同实现
### 多态的实现原理 以及 多态的优点
动态多态(运行时多态):通过虚函数实现，一个类只有一张虚函数表，每个对象有一个虚函数指针。父类指针或引用指向子类对象时，运行时通过对象的虚函数指针找到虚函数表，调用子类重写的函数，实现运行时多态。
静态多态(编译时多态):通过函数重载或模板实现，函数重载是函数名相同，参数个数、类型。顺序不同；模板一套代码适配任意类型。
优点:用父类的统一接口，子类进行功能扩展
### final 标识符的作用是什么？
修饰类不能继承；修饰虚函数不能重写；修饰变量不能修改。
### 虚函数是怎么实现的？它存放在哪里在内存的哪个区？什么时候生成的
有虚函数的类，编译器自动生成一张虚函数表，对象有虚函数指针指向虚函数表，父类指针或引用指向子类对象时，通过子类对象的虚函数指针找到虚函数表，调用子类重写的函数。
虚函数和普通函数一样在代码区
虚函数本身、虚函数表：编译阶段生成
虚指针：编译期确定内存占位，构造函数执行时完成初始化赋值
虚函数调用绑定：运行阶段动态决议
### 智能指针的本质是什么，它们的实现原理是什么？
本质是封装原始C++指针的类
原理是通过对象生命周期和析构函数自动释放堆内存，通过引用计数实现共享
### 匿名函数的本质是什么？优点是什么？
本质是编译器自动生成的匿名仿函数类，实例化为临时对象，重载()运算符，可以进行函数调用
优点是不要函数声明和定义，用完自动销毁，节省空间
### 右值引用是什么，为什么要引入右值引用？
是给临时变量取别名
为了支持移动语义，避免临时变量频繁拷贝，还能实现完美转发
### 左值引用和指针的区别？
逻辑层面：引用是变量的别名，不独立占用空间，和原变量共用同一地址。
实现层面：引用本质是常量指针，会占用和指针一样的内存空间，只是被编译器隐藏了。
初始化:指针可以不初始化，引用必须初始化
性质不同:指针是变量，引用是给对象取别名
占用内存空间不同:指针有自己的内存空间，引用和被引用对象占同一空间
### 指针是什么？
指针是用来存放地址的变量
### weak_ptr真的不计数？是否有计数方式，在哪分配的空间
计数，但是不影响强引用计数use_count(不影响对象生命周期)，会操作弱引用计数weak_count(管理控制块生命周期);
计数存在堆上共享控制块，weak_ptr只在栈上存一个控制块指针;
shared_ptr 如果是使用make_shared初始化，控制块和对象在同一块堆空间
shared_ptr 如果是用new构造shared_ptr，控制块和对象在两块堆空间
### define和const的区别是什么？
阶段:define在预处理阶段进行简单文本替换;const在编译阶段确定其值
安全性:define没有数据类型，不会进行类型安全性检查;const有类型，会进行类型判断
内存占用:define纯文本直接替换，没有独立内存空间;全局、静态const在数据区，局部const在栈区
调试:define不能调试;const可以调试
### 程序运行的步骤是什么
预处理:处理头文件，进行宏替换，生成.i文件
编译:做词法分析、语义分析、错误检查，将源码翻译成汇编语言，生成.s文件
汇编:汇编器将汇编语言翻译成机器语言，生成.o文件
链接:将目标文件和库文件链接到一起，生成可执行文件.exe
运行:可执行文件加载到内存，创建进程，CPU执行指令(断点调试 gdb调试)
### 程序生命周期
编译阶段 链接阶段 加载阶段 运行阶段




### 静态编译和动态编译分别是什么
静态编译:链接时把库代码打包进程序，独立运行，exe体积大，库不共享
动态编译:链接时只做标记，运行时加载外部库，exe体积小，库可共享，依赖环境
### 什么是原子操作
指不会被线程调度机制打断的操作，这种操作一旦开始，就一直运行到结束，中间不会切换到另一线程
### 锁的底层原理是什么？
利用CPU硬件提供的原子指令，保证同一时刻只有一个线程进入临界区，实现并发互斥
CAS机制:三个参数(内存地址V, 旧预期值A, 新值B) 读取内存地址V当前值，比较当前值和旧预期值，相等则更新成新值B，返回成功；不相等则不做修改，返回失败
CAS机制就是自旋锁和互斥锁的底层实现方式
自旋锁 = 死循环 + CAS
while(CAS(lock, 0, 1)){}
抢不到就原地自旋轮询，不放弃CPU
互斥锁 = 自旋锁 + 休眠/唤醒线程
先CAS自旋抢一会，抢不到，线程阻塞休眠，让出CPU交给内核调度；锁释放后，唤醒线程，再用CAS抢锁
### class与struct的区别
默认继承权限不同:class默认继承private权限;struct默认继承public权限
### 内存对齐是什么？为什么要进行内存对齐？内存对齐有什么好处？
内存对齐:把变量的内存地址按照特定整数倍数存放
CPU读取内存是按照块批量读取；兼容不同指令集架构的CPU
减少内存访问次数
### 进程之间的通信方式有哪些？
管道:半双工通信，一个进程读，一个进程写 不适合进程间频繁交换数据
消息队列:可以边发边收，支持按消息类型收发 存在用户态和内核态数据拷贝问题
共享内存:速度最快 解决消息队列用户态和内核态数据拷贝问题
信号量:用于多进程间的同步、互斥，保护临界区资源
信号:异步软中断，用于进程通知、终止、异常处理
套接字:支持本机和跨网络通信，TCP和UDP都是基于套接字实现
### 线程之间的通信方式有哪些？
信号量:用计数器管理资源数量
条件变量:搭配互斥量使用，让线程等待/唤醒
互斥量:保证同一时刻只有一个线程访问临界区资源
### 静态成员变量什么时候赋值，普通成员变量呢
静态成员变量类外全局域编译阶段初始化赋值，程序运行前就分配内存，仅初始化一次。
普通成员变量对象创建、构造函数执行时初始化赋值，每个对象各自独立。
### 父类的构造函数和析构函数是否能为虚函数？这样操作导致的结果？
父类的构造函数不能为虚函数:调用虚构造函数需要虚指针，但是虚指针要等构造函数执行完才能初始化
父类的析构函数可以且经常为虚函数:继承情况下，父类析构函数必须设为虚函数，不然当父类指针指向子类对象时，只会调用父类析构函数，子类内存泄漏
### 多线程为什么会发生死锁，死锁是什么？死锁产生的条件，如何解决死锁？
多进程对资源进行竞争，如果一个进程集合的每个进程都在等待这个集合中的另一个进程才能继续往下执行，没有外力无法继续执行，这就是死锁
条件:互斥 请求和保持 不可剥夺 环路等待
破坏其中一个条件
### 描述一下面向过程和面向对象
面向过程:将问题分析出解决问题的步骤，将步骤一个一个实现，逐个调用
面向对象:将问题分解为各个对象，建立对象是为了描述某个事物在整个解决问题的步骤中的行为
### C++中左值和右值是什么？++i是左值还是右值，++i和i++哪个效率更高？
左值:有内存地址，能取地址
右值:临时值，无固定内存地址，不能取地址
++i是左值
++i返回的是左值，没有发生拷贝，效率高





### 补充
1. 智能指针的实现
智能指针是封装原始指针的类模板
unique_ptr
成员只有一个原始指针
delete 拷贝构造 & 赋值，从语法层面杜绝共享
靠移动语义转移资源所有权，原智能指针置空
析构直接delete指向内存，出作用域自动释放
shared_ptr
内部维护一个原始指针和一个共享的引用计数
拷贝时计数 + 1 析构时计数 - 1
当计数变为 0 才真正释放对象内存
线程安全指计数安全（原子操作），对象访问不安全（指向的实际数据读写线程不安全）
2. lambda函数实现
标准语法格式：
[捕获列表](参数列表) mutable -> 返回值类型 {函数体}
3. 静态编译和动态编译是怎样的
静态编译：把库一起打包进 exe → 大、独立、不依赖环境
动态编译：只留个记号，运行时再找库 → 小、依赖环境、库可以共享
用 windeployqt 打包出来的文件夹里：有一个 .exe旁边跟着一大堆 .dll 文件（Qt5Core.dll、Qt5Gui.dll 等）这就是标准的动态编译！
先正常用 Qt 编译程序,用 windeployqt 把所有 dll 复制出来,打开 Enigma Virtual Box（免费小工具）,把你的 exe 拖进去,把所有 dll、plugins、平台文件全部添加进去,点 打包,最终得到 1 个独立 EXE！无任何依赖！
4. 进程通讯共享内存怎么实现
操作系统创建一块公共内存块，多个进程通过映射把这块内存挂到自己的地址空间，进程直接读写这块内存，就完成了进程间通信。
写入端代码
// 1. 创建一块共享内存（系统内核开辟）
HANDLE hShareMem = CreateFileMappingW(
    INVALID_HANDLE_VALUE,
    NULL,
    PAGE_READWRITE,
    0,
    MEM_SIZE,
    L"MyShareMem001"  // 共享内存名字
);
// 2. 把共享内存映射到当前进程的地址空间
char* pBuf = (char*)MapViewOfFile(
    hShareMem,
    FILE_MAP_ALL_ACCESS,
    0,0,MEM_SIZE
);
// 3. 往映射过来的内存里写数据
strcpy_s(pBuf, MEM_SIZE, "你好共享内存！");
读取端代码
// 1. 通过名字打开同一个共享内存
HANDLE hShareMem = OpenFileMappingW(
    FILE_MAP_ALL_ACCESS,
    FALSE,
    L"MyShareMem001"  // 同一个名字！
);
// 2. 映射到当前进程
char* pBuf = (char*)MapViewOfFile(
    hShareMem,
    FILE_MAP_ALL_ACCESS,
    0,0,MEM_SIZE
);
// 3. 直接读内存
cout << pBuf << endl;
5. 互斥量 条件变量 信号量怎么用
工具			作用					关键字
互斥量 mutex	保护共享数据，不混乱	lock / unlock
条件变量 cv	线程等待条件满足		wait / notify
信号量 sem	控制同时运行的线程数	acquire / release
互斥量作用：多个线程同时访问一份数据，保证只有一个线程在操作，不混乱。
条件变量作用：线程等待某个条件满足，再继续执行，不浪费 CPU。
信号量作用：信号量本质就是 P、V 操作
作用一：限流（控制并发线程数）
作用二：同步（控制线程执行顺序）
作用三：互斥（保护共享资源，等价互斥锁）
6. i++ 和 ++i的实现
// 前置++ 先自增，再返回自身引用,是左值 支持连续++(++c)
Complex& operator++(){
	this->real += 1;
	return *this;
}
// 后置++ 先取值，后自增，返回值，是右值 不能连续后置(a++)++
Complex operator++(int){
	Complex c = *this;
	this->real += 1;
	return c;
}
7. 消息队列怎么实现
消息队列就是操作系统内核里的一个 “消息链表”，不同进程可以往里面发消息、取消息，实现进程间通信。
8. 管道怎么实现
管道：内核缓冲区，半双工、字节流、无边界、易粘包；匿名管道只能父子 / 亲缘进程，命名管道可任意进程；进程退出即消失。
9. 信号怎么用
信号是内核给进程发的 “软中断”，异步、只传编号、不传数据，用来做通知 / 终止 / 重启 / 超时。





### 介绍一下vector、list的底层实现原理和优缺点
vector 底层由一块连续的内存空间组成，由三个指针实现的分别是头指针start、尾指针finish、可用空间尾指针end_of_storage
内存增长规则:当finish == end_of_storage时，申请2倍大小新连续内存，拷贝数据，释放旧内存，更新三个指针
优点:可以下标随机访问，尾插尾删效率高
缺点:前面部分插入删除效率低；扩容效率低
list 底层双向链表实现
优点:按需申请内存，不要扩容，任意位置插入删除效率高
缺点:不支持下标访问
### 静态变量在哪里初始化？在哪一个阶段初始化？
静态变量、全局变量、全局常量，编译确定布局，程序加载阶段完成内存分配与初始化；普通局部变量、类普通成员变量，编译只做语法校验，运行阶段分配内存并初始化。
### 如何实现多进程？
linux中C++使用fork函数创建进程
windows中C++使用createprocess来创建进程
### 空对象指针为什么能调用函数？
类的普通成员函数和静态成员函数，编译后都放在类的外部，所有对象共享一份代码，节省内存；
普通成员函数自带隐式this指针，调用时把对象指针传给this；空对象指针调用时this为空，只要函数内部不访问成员变量，就不会解引用空指针，可以正常执行；
静态成员函数没有this指针，与对象无关，也可以空指针调用；
### shared_ptr线程安全吗？
shared_ptr的引用计数是线程安全的，但是指向的对象不是线程安全的，指针自身赋值、修改指向不是线程安全的
### push_back()左值和右值的区别是什么？
如果push_back()传左值，则使用它拷贝构造新对象
如果push_back()传右值，则使用它移动构造新对象，不拷贝效率高
vector<string> vec;
string s = "hello";
vec.push_back(s); // s是左值，拷贝一份存入容器
vector<string> vec;
vec.push_back("world");       // 字符串临时右值
vec.push_back(string("test"));// 匿名临时对象右值
string s = "data";
vec.push_back(std::move(s)); // 强制转为右值，触发移动
### move底层是怎么实现的
Move的功能是将一个左值引用强制转化为右值引用，通过右值引用使用该值，用于移动语义，实现原理等同强制类型转换
将左值变为右值，避免拷贝构造
### 完美转发的原理是什么
完美转发是函数模板将自己的参数完美转发给内部调用的其他函数，完美是说能保证转发参数的左值右值属性不变
### 空类中有什么函数
C++03 阶段：确实是图中说的 6 个（构造、拷贝构造、赋值、析构、两个取址运算符）
C++11 及以后：新增了移动构造函数和移动赋值运算符，这两个也会在特定条件下自动生成
此时常说的 “六大默认特殊成员函数” 指：默认构造函数、默认拷贝构造函数、默认析构函数、拷贝赋值运算符、移动构造函数、移动赋值运算符
注意：取址运算符不属于标准定义的 “特殊成员函数”，只是可重载的运算符，编译器不会强制生成，也不算在 “六大” 里。
class A
{
public:
    // 1. 默认构造函数
    A(){}
    // 2. 拷贝构造函数
    A(const A&){}
    // 3. 析构函数
    ~A(){}
    // 4. 拷贝赋值运算符
    A& operator=(const A&){return *this;}
    // 5. 移动构造函数
    A(A&&){}
    // 6. 移动赋值运算符
    A& operator=(A&&){return *this;}
};
### explicit用在哪里？有什么作用？
用于修饰只有一个参数的构造函数或者能接收一个参数的多参构造函数，写在构造函数前面，禁止编译器做隐式类型转换
类构造函数默认情况下声明为implicit
### 成员变量初始化的顺序是什么
初始化顺序看声明顺序，与列表顺序无关
const成员变量、引用成员变量必须列表初始化
static必须在类外初始化
全局/静态先初始化，然后父类成员变量初始化，子类成员变量初始化，最后执行构造函数体
### 指针占用的大小是多少
64位电脑上占8字节，32位电脑上占4字节
计算机多少位是指计算机CPU通用寄存器一次性处理、传输、保存的信息的最大长度，8bit就是1字节
### 野指针和内存泄漏是什么？如何避免？
野指针是指向非法内存地址的指针 指针指向的内存已经释放、还没分配、或者不属于当前程序
内存泄漏是程序中动态分配的堆内存没有正常释放，造成系统内存浪费
避免要对指针进行初始化，指针用完释放内存，将指针置空
### malloc和new的区别是什么
malloc是标准库函数 new是C++运算符
malloc分配内存失败返回空 new失败抛出异常
new/delete会调用构造析构函数
malloc返回没有类型的指针 new返回有类型的指针
malloc从堆上内存动态分配 new是从自由存储区上动态分配内存(取决于operator new的实现，可以为堆也可以是静态存储区)
### 多线程会发生什么问题？线程同步有哪些手段？
引发资源竞争问题，甚至发生死锁
使用原子变量保护单个变量 使用互斥量对线程进行上锁 使用条件变量让线程等待 使用信号量控制进入临界区线程的数量
### 什么是STL
标准模板库:容器、迭代器、算法、仿函数、空间配置器、配接器
容器通过空间配置器分配内存存数据，迭代器遍历容器，算法依托迭代器操作容器元素，仿函数给算法提供自定义比较规则，配接器对容器/迭代器/仿函数做适配改装












## 注：
### 智能指针有哪些？怎么使用？
三种 unique_ptr shared_ptr weak_ptr
1. unique_ptr 独占所有权，不能拷贝构造函数、不能拷贝赋值运算符，只能移动构造、移动赋值
2. shared_ptr 引用计数机制，每多一个指针引用，计数+1，指针销毁，计数-1，计数为0自动释放内存
3. weak_ptr 配合shared_ptr使用，解决循环引用造成内存泄漏
unique_ptr<int> up = make_unique<int>(100);
unique_ptr<int> up2 = move(up);
shared_ptr<int> sp1 = make_shared<int>(200);
cout << sp1.use_count();
shared_ptr<int> sp2 = sp1;
weak_ptr wp = sp1;
shared_ptr tmp = wp.lock();
if(tmp){
    cout << *tmp;
}
4. 循环引用问题
两个shared_ptr管理的类对象，互相持有对方的shared_ptr，你指着我，我指着你，形成环形依赖，引用计数永远减不到0，造成内存泄漏。
解决办法:用weak_ptr代替其中一个shared_ptr
### 匿名仿函数类是什么？
编译器给lambda匿名函数自动生成的、没有类名、重载了()运算符的临时类。
auto f = [](int a){ cout << a; };
等价于
class 无名类名{
public:
    void operator()(int a) const{
        cout << a;
    }
};
无名类名 f;
### 右值引用&&怎么用在移动语义和完美转发的？
1. 移动语义:偷资源，不拷贝
// 移动构造
String(String&& other){
    data = other.data;
    other.data = nullptr;
    cout << 移动构造;
}
// 传临时右值触发移动构造
String s2(String("world"));
// 左值强转右值，触发移动构造
String s3(move(s1));
2. 完美转发:保留左值右值属性，原样转发
左值:有名字，能取地址
右值:没名字，临时值
不要完美转发，不管传左值还是右值，到了内层全变成左值，没法匹配重载的右值版本函数
void func(int& x){cout << "左值:" << x;}
void func(int&& x){cout << "右值:" << x;}
template<typename T>
void wrapper(T&& t){
    func(forward<T> t);
}
int a = 10;
wrapper(a);
wrapper(20);
### 阻塞IO和非阻塞IO
阻塞IO:没有数据就阻塞等待，直到有数据才返回
非阻塞IO:有没有数据都返回，需要不断轮询有没有数据，但是一个线程可以轮询多个连接
### this指针
常量指针 指针指向不能改变，指针指向的值可以改变
this指针是类的非静态成员函数隐含的第一个形参，指向当前调用这个函数的对象本身
三大作用
1. 区分成员变量和局部重名变量
2. 返回当前对象本身，支持链式调用
3. 在成员函数中访问当前对象成员
this就是当前对象的地址，由编译器隐式传入非静态成员函数
### 线程安全
多线程同时访问、操作同一份代码或数据，不会出现数据竞争、逻辑错乱就是线程安全
### C++模板
模板函数通常在头文件定义，因为模板的实现需要在编译时可见
### 访问已释放的内存
是未定义行为，导致程序崩溃或输出不确定的值
### 异常
try不能单独使用，必须加catch
catch无法捕获硬件异常、未定义行为异常
throw可以抛出任意类型的值，不只是异常
异常处理会增加运行时开销
### map
map底层是红黑树 unordered_map底层是哈希表
插入需要维护树的平衡时间复杂度O(logn) 哈希表插入时间复杂度是O(1)
### 虚函数
基类指针指向派生类对象时，如果基类析构函数不是虚函数，只会调用基类的析构函数
### 智能指针
std::auto_ptr已被弃用，C++11推荐使用std::unique_ptr
### lambda表达式
lambda表达式支持返回类型自动推导
lambda表达式可以捕获局部变量，通过捕获列表[]获取外部作用域的局部变量
lambda表达式本质是匿名可调用对象，可以直接作为参数传递给标准库算法
lambda递归调用
std::func<int(int)> fact = [&](int n){
    return n <= 1 ? 1 : n * fact(n - 1);
}
### C++多线程
std::thread是C++11引入的
std::mutex不支持递归锁定，不支持对一个线程重复加锁
std::atomic就是为基本数据类型设计的，让这些类型的读写操作变成原子操作
### 命名空间
命名空间可以嵌套
命名空间可以包含类和函数
using namespace会引用命名空间的所有符合
命名空间可以解决命名冲突
### C++类型转换
static_cast 支持多态类型的向下转换，但不做运行时类型检查，而dynamic_cast会做运行时检查，转换失败返回nullptr
dynamic_cast只能用于包含虚函数的多态类，dynamic_cast在基类有虚函数时可以向下转型，把父类指针转换为子类指针
const_cast是移除/添加const/volatile限定符，不能修改真正的常量值，能用来修改const引用/指针指向的非const变量
reinterpret_cast是最不安全的转换，直接把内存的二进制数据解释为目标类型，不做检查
### const/volatile
const:写保护，不能修改变量值
volatile:读不优化，给编译器的禁止优化指令，每次都要从内存读变量值
### C++11的移动语义
std::move将左值转换为右值引用
移动构造函数通常接受非const的右值引用
移动构造可以避免不必要的深拷贝
std::unique_ptr支持移动语义
# C++模板元编程
利用 模板递归 + 全特化 实现编译期计算
主模板负责递归，特化模板是终止条件
计算在编译阶段完成，运行时无开销
// 主模板
template<int N>
struct factorial{
    static const int value = factorial<N - 1>::value;
}
// 特化模板
template<>
struct factorial<0>{
    static const int value = 1;
}
### std::forward
用于完美转发，配合函数模板参数推导保持参数的左值、右值属性
### std::thread
C++必须在std::thread销毁前调用join()或detach()，否则程序会终止
join() 阻塞等待线程执行完毕，线程结束、释放资源
detach() 把C++线程对象和系统线程断开连接，线程自己后台运行
调用后t.joinable()变成false
### constexpr
constexpr 是一个修饰符，用在函数上表示纯函数:相同输入必须得到相同输出，无副作用
constexpr 修饰函数，可以在编译器执行计算
constexpr 函数C++11必须只有一条返回语句
constexpr 函数可以调用其他 constexpr 函数
constexpr 函数参数和返回值必须是字面类型-可以用来计算的类型
constexpr 函数不允许修改非局部变量
### decltype获取类型
#include <type_traits>
is_same_v<A, B>判断A、B类型是否相同
### RAII机制-资源获取就初始化
通过构造函数获取资源，析构函数释放资源
RAII是C++中基于C++对象的生命周期(栈对象自动销毁的特性)的手动资源管理机制
RAII核心定义:资源的获取和对象的初始化绑定在一起，资源的释放和对象的销毁绑定在一起
不仅用于内存管理，还用于文件句柄std::fstream、锁std::lock_guard、网络连接等需要手动释放的资源
C++标准库几乎所有资源都是基于RAII实现的，如string vector thread 智能指针
### std::atomic
只能用于基础数据类型变量
### std::variant
安全的联合体，能存多种类型的其中一种，同一时刻只能存一个类型
### 对象切片
当子类对象按值传递给父类形参时:即使子类重写了虚函数，也只会调用基类函数，要想实现多态必须改为指针或引用传递
### noexcept
这个函数保证不会抛出异常，抛出异常会调用std::terminate
帮助编译器优化代码
是函数签名的一部分，影响函数重载
### mutable
mutable修饰的变量，哪怕是const，也能修改
### std::optional
专门用于表示可能有值可能无值的场景
### alignas alignof
alignas 强制变量的内存对齐字节数
alignof 查询变量的内存对齐字节数
### string_view
不拥有字符串，只指向已有的字符串，只读
### concept
给模板加限制条件，用于模板参数的约束
可以在任何作用域定义
### is_integral_v<T>
判断T是不是整型
### std::jthread
自动调用joint()
支持协作式中断
无需手动资源管理
### 为什么静态成员函数不能定义为虚函数
静态成员函数属于类，虚函数属于对象
### 内联函数
inline 编译时把函数直接嵌入函数调用处
### 为什么析构函数不能重载
析构函数没有参数，不满足重载条件
### 访问
vector 随机访问
list 前后遍历
map/unordered_map 按键访问
### inline函数
适合代码量小、无循环、无递归的函数
代码量大、有循环、有递归的函数编译期展开时，代码严重膨胀
### 5个C++不能重载的运算符
. * 作用域:: 三元运算符 sizeof
### override 
标记重写虚函数的关键字
### 构造函数
可以重载 不能static(this) 可以inline
### 析构函数
不能重载 不能stctic(this) 可以inline
### 访问权限继承后的访问变化
基类权限    public继承  protected继承   private继承
public      public      protected       private
protected   protected   protected       private
private     不可访问    不可访问        不可访问
### 隐藏
派生类定义了和基类同名的函数，但没有构成重写，就会把基类同名函数屏蔽掉，构成隐藏
### 迭代器失效场景
string(动态字符数组)
vector(动态数组) 扩容全失效 中间插入删除当前和后面全失效
list(双向链表) 仅被删除的迭代器失效
map/set(红黑树) 仅被删除的迭代器失效
unordered_map/unordered_set(哈希表) 扩容全失效 仅被删除的迭代器失效
### 重载运算符不能改变的
优先级 操作数 结合性
### 为什么引用不能作为数组元素
数组元素是可以赋值，可以改变的；引用必须初始化，不能改变指向；
### 内存抖动的根本原因
内存中的进程太多了，页面频繁换入换出
### 有效防止内存抖动的方法
减少多道程序度也就是减少内存中的进程数


### 补充
1. 实现Lambda递归调用需要借助哪个工具（）
std::function<int(int)> fact = [&](int n) {
    return n <= 1 ? 1 : n * fact(n - 1);
};
2. C++模板的实例化发生在哪个阶段
模板只是代码模板框架，没有具体类型，编译时传入实参类型，编译器才生成对应专属代码，该过程就是模板实例化。
链接、运行阶段不会再生成模板代码，模板必须头文件可见，才能在编译期完成实例化。
实例化后才会产生真正可执行的函数 / 类实体。



## 二、C++课程
1. 属性私有化就是把类里的变量（属性）藏起来，不让外部直接访问、修改，只能通过类提供的公开方法间接操作。
它是 C++ 面向对象三大特性之一：封装 的核心思想。
2. 拷贝构造函数的定义
类名(const 类名 &对象名)
{
    拷贝代码
}
3. 拷贝构造函数的调用时机：新建对象时，拿另一个同类对象来赋值初始化，就调用拷贝构造。
使用已有对象初始化新创建的对象
函数的传参
函数的返回值
匿名对象初始化
4. 静态成员变量
所有对象共享同一份数据
编译阶段分配内存，数据段（静态区/全局区）
类内声明，类外初始化
5. 静态成员函数
所有对象共享函数
静态成员函数只能使用静态成员变量，无法使用普通成员变量
6. this指针
解决命名冲突
*this可以获取对象本身
7. const修饰成员函数-常函数 const修饰对象-常对象
不能修改成员变量的值
常对象只能调用常函数
8. mutable 可变的
常函数中修改成员变量的值
9. 友元
让一个类或者函数能够访问另一个类的私有成员
全局函数作为友元
类作为友元
成员函数作为友元
10. 友元定义时的顺序
先定义友元类/函数再定义基本类
11. + -运算符重载
// 成员函数
Complex operator+(Complex& b)
{
	Complex ret;
	ret.real = this->real + b.real;
	ret.image = this->image + b.image;
	return ret;
}
Complex operator-(Complex& b)
{
	Complex ret;
	ret.real = this->real - b.real;
	ret.image = this->image - b.image;
	return ret;
}
// 全局函数
Complex operator+(Complex& a, Complex& b){
	Complex ret;
	ret.real = a.real + b.real;
	ret.image = a.image + b.image;
	return ret;
}
Complex operator-(Complex& a, Complex& b){
	Complex ret;
	ret.real = a.real - b.real;
	ret.image = a.image - b.image;
	return ret;
}
12. << 左移运算符重载
ostream& operator<<(ostream& cout, const Complex& a){
	cout << a.real << '+' << a.image << 'i';
	return cout;
}
13. 重载函数类型
<< 左移：只能全局 / 友元，不能成员
>> 右移：同上，也不能成员
+ - * /  = == 等：既能成员，也能全局
14. 不想让函数执行拷贝构造
拷贝构造函数 = delete
15. 自增重载
// 前置++ 先自增，再返回自身引用,是左值 支持连续++(++c)
Complex& operator++(){
	this->real += 1;
	return *this;
}
// 后置++ 先取值，后自增，返回值，是右值 不能连续后置(a++)++
Complex operator++(int){
	Complex c = *this;
	this->real += 1;
	return c;
}
16. 函数返回值是左值还是右值
看返回值类型，返引用 = 左值，返普通值 = 右值
17. 赋值运算符重载
Hero& operator=(Hero& h){
	if(m_Data){
		delete m_Data;
		m_Data = nullptr;
	}
	m_Data = new int;
	*m_Data = *h.m_Data;
	return *this;
}
18. C++ 常函数
成员函数后面加 const，就是常成员函数
void show() const{}
19. 关系运算符重载 == != > >= < <=
bool operator==(const Point& other) const{
	return m_x == other.m_x && m_y == other.m_y;
}
bool operator>const Point& other) const{
	int d = m_x * m_x + m_y * m_y;
	int otherd = other.m_x * other.m_x + other.m_y * other.m_y;
	return d < otherd;
}
bool operator>const Point& other) const{
	int d = m_x * m_x + m_y * m_y;
	int otherd = other.m_x * other.m_x + other.m_y * other.m_y;
	return d > otherd;
}
20. 函数调用运算符重载 仿函数能存状态
int operator() (int a, int b){
	return a + b;
}
int add(int a, int b){
	return a + b;
}
21. for_each
for_each 是 STL 遍历算法，用来遍历容器（数组 /vector 等），对每个元素执行你指定的操作。
#include <algorithm>
for_each(起始迭代器, 结束迭代器, 函数/仿函数); 第三个参数只写函数名，不加 ()
配合仿函数带状态，有()
for_each(v.begin(), v.end(), Print());
22. 继承方式
基类成员		公有继承 			保护继承 			私有继承 
public		public			protected		private
protected	protected		protected		private
private		不可访问			不可访问			不可访问
23. 同名属性访问
父类和子类有相同名字的成员变量，不会覆盖，存储在不同的地址中
子类作用域优先，直接访问默认优先访问子类
想访问父类同名成员，必须加作用域:子类对象.父类名::成员名;
24. 同名函数访问
父子类同名函数不会覆盖、不会替换，各自独立存在，地址不同
直接调用 优先调用子类
想调用父类同名函数，必须加类名作用域：子类对象.父类名::函数名();
25. 多继承 —— 多个父类有同名属性 / 同名函数
不会冲突，不会覆盖
每个父类的同名成员各自独立存一份，占用各自内存
直接访问会报错（二义性），必须加 父类作用域 区分
26. 子类对象隐式转换成父类对象
子类 → 父类（向上转型）允许隐式自动转换，安全无风险对象、指针、引用都能直接转，发生对象切片
父类 → 子类（向下转型）禁止隐式转换，编译器直接报错只能手动强制类型转换，风险极高纯父类对象强转后，访问子类独有成员极易崩溃
27. 什么是多态
存在公有继承，父类声明虚函数，子类完成函数重写；用父类指针或父类引用指向子类对象，调用同名虚函数时，不会执行父类版本，而是执行子类重写后的专属实现，这就是 C++ 动态多态。
公有继承 public父类指针 / 引用 可以合法指向子类对象，满足向上转型，能触发多态。
保护继承 protected、私有继承 private
子类对外隐藏父类，外部无法把子类对象转成父类指针 / 引用
做不了向上转型，直接无法构成多态
28. 没有任何成员变量的类的大小是多少，为什么
大小是 1 字节
C++ 规定：任何对象都必须有独一无二的内存地址。
但如果大小是0，两个不同的对象地址会完全一样
编译器为了区分不同对象，强制给空类分配 1 个字节
29. 类的函数占用类的内存吗
普通成员函数不占用对象内存，仅虚函数会额外占一个虚函数指针大小内存。
30. 纯虚函数 纯虚函数的类是抽象类
virtual void func() = 0;
抽象类不能实例化，不能创建对象
派生类必须重写所有纯虚函数，否则派生类也还是抽象类
31. 虚析构和纯虚析构
虚析构：基类指针删除派生类对象时，保证子类析构函数正常调用，避免内存泄漏。
纯虚析构必须类外补充函数体
有纯虚析构后，类变为抽象类，不能实例化，可以父类指针指向子类对象
32. 函数模板
template<typename T>
T add(T a, T b)
{
    return a + b;
}
add(1,2);
add(1.1,2.2);
33. 函数模板的调用两种方式
自动类型推导：编译器自动看你传什么参数，自动推导出 T 的类型
显式指定类型：手动写 <类型> 告诉编译器 T 是什么
34. 普通函数有隐式类型转换
int func(int a){ return a; }
func(3.14); // double隐式转int，合法
35. 函数模板自动推导下不允许隐式转换，类型必须严格匹配
template<typename T>
void show(T a, T b)
{}
// 1. 类型一致，推导成功
show(1, 2);
show(1.5, 2.6);
// 2. 整型+浮点型，类型不统一，自动推导报错
show(1, 2.5);
36. 模板显式指定类型后，可触发类型转换
show<int, int>(1, 2.5); // 转为int类型调用
37. 模板参数类型使用typename和使用class有什么区别
语法上无区别，功能完全等同
唯一区别:嵌套依赖类型必须用 typename，不能用 class
// 容器迭代器场景
template<class Vec>
void func(Vec& v)
{
    // 嵌套依赖类型:模板类型参数名 :: 内部类型，只能用typename
    typename Vec::iterator it = v.begin();
}
编译器无法立刻判定Vec::iterator是类型还是静态成员变量，typename 强制告知这是一个类型，class 无此语法作用。
38. 优先调用普通函数
同名的普通函数和函数模板同时存在，优先执行普通函数。
39. 强制调用函数模板
自动类型推导 show<>(1.5, 2.6);
显式指定类型 show<int, int>(1, 2.5); 
40. 函数模板的优先匹配
优先级1：普通函数 最高！
只要有精准匹配的普通函数，永远优先调用它。
void func(int a) { ... } // 最高
func(10); // 一定走普通函数
优先级2：函数模板（更精准 / 更特化）
两个模板同时存在时，越具体、越精准，优先级越高。
template<T> void func(T a);       // 模板1（通用）
template<T> void func(T* a);      // 模板2（指针更精准）
int a = 10;
func(&a); // 走模板2，因为指针更匹配
优先级3：普通模板（最不精准）
没有普通函数、没有更精准模板时，才调用它。
41. 多参数函数模板
返回值类型无法作为推导依据
有类型推导不出来就会编译失败
typeid(T).name() 查看类型
42. 变量初始化规则
局部变量（栈区）不会自动初始化！值是随机垃圾数！
全局变量（全局 / 静态区）会自动初始化为0！
静态局部变量 static 也会自动初始化为0！
43. 类模板
vector 底层实现
44. vector<char> 和 string 有什么区别
vector<char>：字符数组，只管存数据，没有字符串操作
string：专门处理字符串，自带字符串函数 + 末尾自动存 \0
45. 类模板成员函数 类内定义 类外定义
类内定义：直接在类里面写函数体，和普通类一样，虚拟类型用T
template<typename T>
class Person
{
public:
    T m_age;
    // 类内定义成员函数
    void show()
    {
        cout << m_age << endl;
    }
};
类外定义：前面必须加模板声明：template<typename T> 类名后面必须加 <T>：Person<T>::
template<typename T>
class Person
{
public:
    T m_age;
    void show(); // 仅声明
};
// 类外实现！！！
template<typename T>  // 1. 必须加模板
void Person<T>::show() // 2. 必须写 Person<T>
{
    cout << m_age << endl;
}
46. 成员函数创建时机
普通类：类定义完成，所有成员函数代码就全部生成，不管是否调用。
类模板：实例化使用时，才创建对应类型的成员函数
47. 类模板对象的函数传参
template<class T>
class Demo{};
直接指定类型
void test(Demo<int> d){}
参数模板化 函数模板
template<class T>
void test(Demo<T> d){}
类模板化 
template<class T>
void test(T& d){}
48. 类模板继承
template<typename T>
class Base {
    T m_a;
};
普通子类 继承 模板父类（指定具体类型）
class Son : public Base<int> {};
模板子类 继承 模板父类（灵活通用）
template<typename T>
class Son : public Base<T> {};
模板继承时，子类不能直接访问父类成员！必须加 this-> 或 Base<T>::
void func() {
    this->m_a = 10;
}
void func() {
    Base<T>::m_a = 10;
}
